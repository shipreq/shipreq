#!/bin/bash

source "$(dirname "$0")"/lib.sh
lookup1 "$@"

s="$sqldir/create-$1.sql"
[ ! -e "$s" ] && die "File not found: $s"

echo "Creating database $db..."
echo "$sudo_psql < $s"
$sudo_psql < "$s"
