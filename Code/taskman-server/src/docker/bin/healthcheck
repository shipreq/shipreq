#!/bin/bash
[ $# -ne 2 ] && echo "Usage: $0 <health file> <max age>" && exit 2

[ ! -e "$1" ] && echo "File not found: $1" >&2 && exit 1

age="$(( $(date +"%s") - $(stat -c "%Y" "$1") ))"
echo "Age: $age"
[ $age -gt "$2" ] && exit 1
exit 0