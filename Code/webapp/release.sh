#!/bin/bash
cd "$(dirname "$0")"
grunt
echo '--------------------------------------------------------------------------------'
git st .
echo '--------------------------------------------------------------------------------'
cd .. && sbt -DMODE=release 'project webapp' clean package test
