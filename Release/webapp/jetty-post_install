#!/bin/bash
cd $(dirname "$0") || exit 1

cd jetty \
  && echo "Removing crap..." \
  && git rm -r */*{jaas,jsp}[.-]* lib/apache-jsp \
  && echo "Applying patches..." \
  && ../jetty-patch-start_patience \
  && echo "Done." \
  && echo && git st .
