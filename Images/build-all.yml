version: 0.2

phases:
  install:
    runtime-versions:
      docker: 18
  pre_build:
    commands:
      - env|sort
      - $(aws ecr get-login --no-include-email --region $AWS_REGION)
  build:
    commands:
      - Images/build-all
