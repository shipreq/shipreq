global:
  scrape_interval: ${PROMETHEUS_BIZ_SCRAPE_INTERVAL}

scrape_configs:

  - job_name: 'postgres_exporter'
    dns_sd_configs:
      - names: [ '${OPS_HOST}' ]
        type: 'A'
        port: ${POSTGRES_EXPORTER_PORT}
